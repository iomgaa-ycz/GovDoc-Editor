# Templates

`workpaper.docx` 必须由 Word 或 LibreOffice 手工制作并保存到本目录。

根据 `docs/design.md` v3 约束：

- 不允许程序化生成模板并冒充正式模板。
- 模板变量名必须严格对齐 `govdoc.schemas.Workpaper.model_dump()`。
- 本仓库初始化阶段只预留目录与说明，正式模板在 `T0.8` 落地。

