# GovDoc Prompts

本目录存放业务 prompt 片段，供 `agents/gov-*.yaml` 的 `prompt_text` 或阶段提示引用。

约束：

- 只写政府采购业务语义，不放通用编排逻辑。
- 业务 prompt 必须与 `govdoc/schemas/` 中的领域模型保持一致。
- 若 prompt 与 `docs/design.md` 冲突，以 design 为准。

