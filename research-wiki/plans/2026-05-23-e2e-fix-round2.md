# E2E 测试第二轮修复实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复全量 E2E 测试中发现的 6 个失败：1 个对比算法 bug、1 个 GPU 资源问题、4 个测试代码问题。

**Architecture:** 先修复对比算法的句子级去重 bug（后端），再校准 fixture 真值并修复测试代码（前端 E2E），最后切 GPU + 部署 + 全量验证。

**Tech Stack:** Python/FastAPI（后端修复）、@playwright/cli（E2E 测试）、SSH/tmux（GPU 切换）

---

### Task 1: 修复对比算法句子级去重 bug

**Files:**
- Modify: `govdoc/compare/service.py:370-413`（_build_nfile_sentence_matches 增加段落去重参数）
- Modify: `govdoc/compare/service.py:742-743`（调用处传入段落范围）

- [ ] **Step 1: 修改 `_build_nfile_sentence_matches` 函数签名和过滤逻辑**

在 `govdoc/compare/service.py` 第 370 行，将：

```python
def _build_nfile_sentence_matches(documents: list[DocumentModel]) -> list[MatchRecord]:
    """在所有文件间查找完全相同的句子。"""
    sentence_lookup: dict[str, dict[int, list[SentenceOccurrence]]] = defaultdict(
        lambda: defaultdict(list)
    )
    first_seen: dict[str, tuple[int, int]] = {}

    for doc in documents:
        for order, sentence in enumerate(_iter_sentence_occurrences(doc), start=1):
            sentence_lookup[sentence.text][doc.file_index].append(sentence)
            first_seen.setdefault(sentence.text, (doc.file_index, order))
```

替换为：

```python
def _build_nfile_sentence_matches(
    documents: list[DocumentModel],
    paragraph_ranges_by_file: dict[int, list[tuple[int, int]]] | None = None,
) -> list[MatchRecord]:
    """在所有文件间查找完全相同的句子（排除已被段落级覆盖的）。"""
    sentence_lookup: dict[str, dict[int, list[SentenceOccurrence]]] = defaultdict(
        lambda: defaultdict(list)
    )
    first_seen: dict[str, tuple[int, int]] = {}

    for doc in documents:
        for order, sentence in enumerate(_iter_sentence_occurrences(doc), start=1):
            if paragraph_ranges_by_file and doc.file_index in paragraph_ranges_by_file:
                if _is_covered_by_ranges(
                    document=doc,
                    start=sentence.start,
                    end=sentence.end,
                    ranges=paragraph_ranges_by_file[doc.file_index],
                ):
                    continue
            sentence_lookup[sentence.text][doc.file_index].append(sentence)
            first_seen.setdefault(sentence.text, (doc.file_index, order))
```

- [ ] **Step 2: 修改调用处，传入段落范围**

在 `govdoc/compare/service.py` 第 742-743 行，将：

```python
    paragraph_matches = _build_nfile_block_matches(documents)
    sentence_matches = _build_nfile_sentence_matches(documents)
```

替换为：

```python
    paragraph_matches = _build_nfile_block_matches(documents)
    paragraph_ranges = _build_exact_ranges_by_file(paragraph_matches)
    sentence_matches = _build_nfile_sentence_matches(
        documents, paragraph_ranges_by_file=paragraph_ranges,
    )
```

- [ ] **Step 3: 运行现有单元测试确认不回归**

```bash
source activate govdoc-auditor-v3 && python -m pytest tests/unit/test_compare_service.py -v
```

Expected: 全部通过（现有测试不依赖重复计数行为）。

---

### Task 2: 增加句子去重单元测试

**Files:**
- Modify: `tests/unit/test_compare_service.py`

- [ ] **Step 1: 在文件末尾追加测试**

```python
def test_sentence_inside_matched_paragraph_is_deduplicated(tmp_path: Path) -> None:
    """段落内的单句子不应被重复计入 sentenceCount。"""
    first_path = tmp_path / "a.docx"
    second_path = tmp_path / "b.docx"
    output_root = tmp_path / "compare"
    _write_docx(first_path, ["唯一共同段落。", "独有A段落。"])
    _write_docx(second_path, ["唯一共同段落。", "独有B段落。"])

    payload = create_compare_bundle(
        files=[(first_path, first_path.name), (second_path, second_path.name)],
        output_root=output_root,
    )

    assert payload.summary.common_paragraph_count == 1
    assert payload.summary.common_sentence_count == 0
    assert not any(m.category == "sentence" for m in payload.matches)


def test_sentence_dedup_preserves_cross_paragraph_sentences(tmp_path: Path) -> None:
    """跨段落的相同句子（不在段落匹配中）应保留。"""
    first_path = tmp_path / "a.docx"
    second_path = tmp_path / "b.docx"
    output_root = tmp_path / "compare"
    _write_docx(first_path, ["第一句共享。第二句不同A。"])
    _write_docx(second_path, ["第一句共享。第二句不同B。"])

    payload = create_compare_bundle(
        files=[(first_path, first_path.name), (second_path, second_path.name)],
        output_root=output_root,
    )

    assert payload.summary.common_paragraph_count == 0
    assert payload.summary.common_sentence_count == 1
    assert any(m.text == "第一句共享。" and m.category == "sentence" for m in payload.matches)
```

- [ ] **Step 2: 运行新测试**

```bash
source activate govdoc-auditor-v3 && python -m pytest tests/unit/test_compare_service.py::test_sentence_inside_matched_paragraph_is_deduplicated tests/unit/test_compare_service.py::test_sentence_dedup_preserves_cross_paragraph_sentences -v
```

Expected: 2 个新测试全部通过。

---

### Task 3: 重新计算 fixture 真值并更新 test-14

**Files:**
- Modify: `frontend/e2e/test-14-compare-verify.js:61-121`

- [ ] **Step 1: 运行 fixture 获取修复后的真值**

```bash
source activate govdoc-auditor-v3 && python3 -c "
from govdoc.compare.service import create_compare_bundle
from pathlib import Path
import tempfile

root = Path('tests/fixtures/compare')
cases = [
    ('①段落级', ['para_a.docx', 'para_b.docx']),
    ('②句子级', ['sent_a.docx', 'sent_b.docx']),
    ('③片段级', ['seg_a.docx', 'seg_b.docx']),
    ('④混合',   ['mix_a.docx', 'mix_b.docx']),
    ('⑤自比较', ['para_a.docx', 'para_a.docx']),
    ('⑥无重复', ['unique_a.docx', 'unique_b.docx']),
    ('⑦多文档', ['multi_1.docx', 'multi_2.docx', 'multi_3.docx', 'multi_4.docx']),
]
for label, files in cases:
    out = Path(tempfile.mkdtemp())
    flist = [(root/f, f) for f in files]
    p = create_compare_bundle(files=flist, output_root=out)
    s = p.summary
    print(f'{label}: p={s.common_paragraph_count} s={s.common_sentence_count} seg={s.common_segment_count} m={s.match_count}')
    for m in p.matches:
        print(f'  [{m.category}] \"{m.text[:50]}\" files={m.file_indices}')
"
```

记录输出的真值。

- [ ] **Step 2: 用真值更新 test-14 的断言**

根据 Step 1 输出的实际数值，更新 `frontend/e2e/test-14-compare-verify.js` 中每个用例的断言。关键是用例①的 `sentenceCount`（修复后应为 0）和 `matchCount`（修复后应减少）。

---

### Task 4: 修复 test-09 编辑后等待

**Files:**
- Modify: `frontend/e2e/test-09-audit-library-crud.js:59-63`

- [ ] **Step 1: 将固定等待改为 waitFor**

将第 59-63 行：

```javascript
  await page.getByRole('button', { name: '保存修改' }).click();
  await page.waitForTimeout(1000);

  const updatedRow = page.getByText(originalTitle + ' [E2E]');
  if (!(await updatedRow.isVisible())) throw new Error('编辑保存后列表未更新');
```

替换为：

```javascript
  await page.getByRole('button', { name: '保存修改' }).click();

  const updatedRow = page.getByText(originalTitle + ' [E2E]');
  await updatedRow.waitFor({ timeout: 10000 });
```

---

### Task 5: 修复 test-12 选择有内容的运行

**Files:**
- Modify: `frontend/e2e/test-12-workpaper-page.js:43-56`

- [ ] **Step 1: 重写 Select 选择逻辑，遍历找有内容的运行**

将第 43-56 行替换为：

```javascript
  // Step 3: 遍历 Select 选项，找有底稿内容的运行
  const selectTrigger = page.locator('header button[role="combobox"]').first();
  let editorLoaded = false;
  if (await selectTrigger.isVisible().catch(() => false)) {
    await selectTrigger.click();
    const selectOptions = page.locator('[role="option"]');
    const optCount = await selectOptions.count();
    for (let i = 0; i < optCount; i++) {
      if (i > 0) {
        await selectTrigger.click();
        await page.locator('[role="option"]').nth(i).click();
      } else {
        await selectOptions.nth(i).click();
      }
      await page.waitForTimeout(2000);
      const editor = page.locator('[contenteditable="true"]');
      if (await editor.isVisible().catch(() => false)) {
        const text = (await editor.textContent() || '').trim();
        if (text.length > 10) {
          editorLoaded = true;
          console.log('PASS: 找到有内容的运行（选项 ' + i + '，' + text.length + ' 字）');
          break;
        }
      }
    }
    if (!editorLoaded) {
      console.log('SKIP: 所有运行均无底稿内容，跳过编辑器测试');
    }
  }
```

然后将后续 Step 4-5（toolbar 和编辑）包裹在 `if (editorLoaded)` 中。

---

### Task 6: 修复 test-13 文件添加/移除逻辑

**Files:**
- Modify: `frontend/e2e/test-13-doc-compare-advanced.js:17-42`

- [ ] **Step 1: 重写 Step 3-5 的文件上传和移除逻辑**

将第 17-42 行替换为：

```javascript
  // Step 3: 上传 1 个文件，按钮仍禁用（需要 ≥2）
  const fileInput = page.locator("input[type='file']").first();
  await fileInput.setInputFiles(DOC_A);
  await page.waitForTimeout(500);
  if (!(await compareBtn.isDisabled())) throw new Error('仅1个文件时开始对比按钮未禁用');
  console.log('PASS: 1 个文件 → 开始对比仍禁用');

  // Step 4: 设置 3 个文件，验证 3 个卡片
  await fileInput.setInputFiles([DOC_A, DOC_B, DOC_A]);
  await page.waitForTimeout(500);
  const fileCards = page.locator('.rounded-card.border.bg-white');
  const cardCount3 = await fileCards.count();
  if (cardCount3 !== 3) throw new Error('应有 3 个文件卡片，实际: ' + cardCount3);
  console.log('PASS: 3 个文件卡片正确');

  // Step 5: 移除第 2 个文件，验证剩 2 个
  const removeButtons = page.locator('button[aria-label*="移除"]');
  if (await removeButtons.count() >= 2) {
    await removeButtons.nth(1).click();
    await page.waitForTimeout(300);
    const cardCount2 = await fileCards.count();
    if (cardCount2 !== 2) throw new Error('移除后应剩 2 个文件卡片，实际: ' + cardCount2);
    console.log('PASS: 文件移除成功（3 → 2）');
  } else {
    console.log('SKIP: 移除按钮不足');
  }

  if (await compareBtn.isDisabled()) throw new Error('2 个文件时按钮仍禁用');
  console.log('PASS: 2 个文件 → 开始对比启用');
```

---

### Task 7: 修复 test-15 导航到有 completed 审核点的运行

**Files:**
- Modify: `frontend/e2e/test-15-result-quality.js:7-28`

- [ ] **Step 1: 重写 Part A 开头的导航逻辑**

将第 7-28 行替换为：

```javascript
  // Part A: 审核结果质量验证
  console.log('== Part A: 审核结果质量验证 ==');

  await page.goto(BASE + '/audit-results');
  await page.waitForLoadState('networkidle');
  await page.waitForTimeout(3000);

  // 遍历 Select 找有 completed 审核点的运行
  const selectTrigger = page.locator('header button[role="combobox"]').first();
  let foundCompleted = false;

  if (await selectTrigger.isVisible().catch(() => false)) {
    await selectTrigger.click();
    const allOptions = page.locator('[role="option"]');
    const optCount = await allOptions.count();
    await page.keyboard.press('Escape').catch(() => {});

    for (let oi = 0; oi < optCount; oi++) {
      await selectTrigger.click();
      await page.locator('[role="option"]').nth(oi).click();
      await page.waitForTimeout(2000);

      const leftPanel = page.locator('.w-80').first();
      const pointButtons = leftPanel.locator('button');
      const pointCount = await pointButtons.count();

      // 检查是否有任何审核点有 verdict
      for (let pi = 0; pi < pointCount; pi++) {
        await pointButtons.nth(pi).click();
        await page.waitForTimeout(800);
        if (await page.getByText('审核结论').isVisible().catch(() => false)) {
          foundCompleted = true;
          break;
        }
      }
      if (foundCompleted) {
        console.log('找到有 completed 审核点的运行（选项 ' + oi + '）');
        break;
      }
    }
  }

  if (!foundCompleted) throw new Error('没有任何审核运行包含已完成的审核点');

  const leftPanel = page.locator('.w-80').first();
  const pointButtons = leftPanel.locator('button');
  const pointCount = await pointButtons.count();
```

---

### Task 8: GPU 切换 + 部署 + 全量测试

**Files:**
- Modify: `scripts/deploy.sh:106`（增加 CUDA_VISIBLE_DEVICES=7）

- [ ] **Step 1: 修改 deploy.sh 增加 GPU 指定**

在 `scripts/deploy.sh` 第 106 行的 uvicorn 启动命令前加 `CUDA_VISIBLE_DEVICES=7`：

将：
```bash
            export no_proxy=110.42.53.85,100.81.95.44,localhost,127.0.0.1 && export NO_PROXY=\\\$no_proxy && \
            cd ~/Project/${dir} && \
            uvicorn govdoc.api.main:app --host 0.0.0.0 --port ${port}; \
```

替换为：
```bash
            export no_proxy=110.42.53.85,100.81.95.44,localhost,127.0.0.1 && export NO_PROXY=\\\$no_proxy && \
            export CUDA_VISIBLE_DEVICES=7 && \
            cd ~/Project/${dir} && \
            uvicorn govdoc.api.main:app --host 0.0.0.0 --port ${port}; \
```

- [ ] **Step 2: 部署到 testing**

```bash
bash scripts/deploy.sh --target testing
```

Expected: 部署成功，后端使用 GPU 7。

- [ ] **Step 3: 验证 GPU 使用正确**

```bash
ssh yuchengzhang@100.83.164.94 "nvidia-smi --query-compute-apps=pid,used_memory --format=csv | head -10"
```

Expected: govdoc 进程在 GPU 7 上。

- [ ] **Step 4: 运行全量 E2E 测试**

```bash
bash frontend/e2e/run-tests.sh 2>&1
```

Expected: 15/15 通过。如有失败，以报告形式阐述问题。
