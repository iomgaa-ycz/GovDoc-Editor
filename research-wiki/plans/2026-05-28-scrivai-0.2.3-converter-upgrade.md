# Scrivai 0.2.3 MarkdownConverter 升级实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 DocumentStore 的文档转换层从已废弃的 `to_markdown()` 旧签名迁移到 `MarkdownConverter` 单例，实现 MonkeyOCR 双端点主力 + GLM OCR fallback。

**Architecture:** `govdoc.yaml` 新增 `converter:` 块，透传为 `ConverterConfig` kwargs → `DocumentStore` 持有一个 `MarkdownConverter` 单例（`lru_cache` 保证进程级复用）→ 上层 `get_or_convert()` 接口不变，routes/compare/harness 零改动。

**Tech Stack:** Scrivai 0.2.3 (`MarkdownConverter`, `ConverterConfig`)、Pydantic v2、pytest

**设计文档：** `research-wiki/designs/2026-05-28-scrivai-0.2.3-converter-upgrade-design.md`

**设计勘误：** `CompareConfig.max_concurrent` 不能删——它控制对比 API 并发（asyncio semaphore），不是 OCR 并发。仅删 `CompareConfig.ocr_backend`。另外 `govdoc/harness/pipeline_eval.py:709` 也直接构造 DocumentStore，需同步更新。

---

### Task 1: 更新配置层（govdoc.yaml + config.py）

**Files:**
- Modify: `govdoc.yaml`
- Modify: `govdoc/config.py:25-31` (AppConfig), `govdoc/config.py:82-92` (CompareConfig)

- [ ] **Step 1: 修改 `govdoc.yaml`**

`app:` 段删除 `ocr_backend: glm`，新增 `converter:` 块。`compare:` 段删除 `ocr_backend: glm`（保留 `max_concurrent`）。

完整新文件内容：

```yaml
app:
  storage_root: ./data/storage
  database_url: sqlite:///./data/app.sqlite
  converter:
    mode: api
    dispatch: fallback
    api_backends: [monkey, glm]
    monkey_endpoints:
      - "http://100.83.164.94:7866"
      - "http://100.83.164.94:7867"
    concurrency_per_group:
      monkey: 3
      glm: 1
    max_failures:
      monkey: 3
    timeout: 600
    chunk_pages: 50
    overlap_pages: 2

model:
  model: glm-5.1
  base_url: "http://110.42.53.85:11098"
  api_key: ${ANTHROPIC_API_KEY}
  fallback_model: glm-5.1

qmd:
  db_path: ./data/qmd.sqlite

workspace:
  workspaces_root: ./data/.govdoc/workspaces
  archives_root: ./data/.govdoc/archives
  cleanup_days: 30

evolution:
  enabled: false
  proposer_model: claude-sonnet-4-6
  frontier_size: 5
  eval_dataset_path: ./data/eval/checkpoints_golden.json

harness:
  db_path: ./results/harness.db
  judge_model: "glm-5.1"
  judge_base_url: "http://110.42.53.85:11098/v1"

audit:
  point_timeout_s: 900

compare:
  max_files: null
  min_segment_length: 16
  pdf_timeout_s: 7200
  simhash_threshold: 10
  max_concurrent: 1
```

- [ ] **Step 2: 修改 `govdoc/config.py` — AppConfig**

将 `ocr_backend: str = "glm"` 替换为 `converter: dict[str, Any] = Field(default_factory=dict)`。

```python
class AppConfig(BaseModel):
    model_config = ConfigDict(extra="forbid")

    storage_root: str = "./data/storage"
    database_url: str = "sqlite:///./data/app.sqlite"
    converter: dict[str, Any] = Field(default_factory=dict)
```

- [ ] **Step 3: 修改 `govdoc/config.py` — CompareConfig**

仅删除 `ocr_backend` 行，保留 `max_concurrent`。

```python
class CompareConfig(BaseModel):
    """文档对比功能配置。"""

    model_config = ConfigDict(extra="forbid")

    max_files: int | None = None
    min_segment_length: int = 16
    pdf_timeout_s: int = 7200
    simhash_threshold: int = 10
    max_concurrent: int = 1
```

- [ ] **Step 4: 验证配置加载**

Run: `source activate govdoc-auditor-v3 && python -c "from govdoc.config import load_config; cfg = load_config(); print('converter:', cfg.app.converter); print('compare.max_concurrent:', cfg.compare.max_concurrent)"`

Expected: `converter:` 输出包含 monkey_endpoints 等字段；`compare.max_concurrent: 1`。

- [ ] **Step 5: Commit**

```bash
git add govdoc.yaml govdoc/config.py
git commit -m "refactor: AppConfig 用 converter dict 替代 ocr_backend，适配 Scrivai 0.2.3"
```

---

### Task 2: 重写 DocumentStore（files.py）

**Files:**
- Modify: `govdoc/storage/files.py`

- [ ] **Step 1: 替换导入**

将第 10 行：
```python
from scrivai import to_markdown as _scrivai_to_markdown
```
替换为：
```python
from scrivai import MarkdownConverter
```

- [ ] **Step 2: 修改 `__init__` 签名和构造**

将：
```python
def __init__(self, storage_root: Path, *, ocr_backend: str = "glm") -> None:
    self._root = storage_root
    self._ocr_backend = ocr_backend
    self._prepared_dir = storage_root / "prepared"
    self._raw_dir = storage_root / "raw"
    self._prepared_dir.mkdir(parents=True, exist_ok=True)
    self._raw_dir.mkdir(parents=True, exist_ok=True)
```
替换为：
```python
def __init__(
    self,
    storage_root: Path,
    *,
    converter_kwargs: dict[str, Any] | None = None,
) -> None:
    self._root = storage_root
    self._converter = MarkdownConverter(**(converter_kwargs or {}))
    self._prepared_dir = storage_root / "prepared"
    self._raw_dir = storage_root / "raw"
    self._prepared_dir.mkdir(parents=True, exist_ok=True)
    self._raw_dir.mkdir(parents=True, exist_ok=True)
```

同时在文件顶部 imports 中添加：
```python
from typing import Any
```

- [ ] **Step 3: 修改 `_convert` 方法**

将：
```python
def _convert(self, raw: Path, target: Path) -> Path:
    """调用 scrivai.to_markdown 将原始文件转换为 markdown。"""
    md = _scrivai_to_markdown(
        raw,
        ocr_backend=self._ocr_backend,
        max_workers=1,
        timeout=7200,
    )
    if not md or not md.strip():
        raise RuntimeError(f"to_markdown 返回空内容: {raw}")
    target.write_text(md, encoding="utf-8")
    return target
```
替换为：
```python
def _convert(self, raw: Path, target: Path) -> Path:
    """调用 MarkdownConverter.convert 将原始文件转换为 markdown。"""
    md = self._converter.convert(raw)
    if not md or not md.strip():
        raise RuntimeError(f"MarkdownConverter.convert 返回空内容: {raw}")
    target.write_text(md, encoding="utf-8")
    return target
```

- [ ] **Step 4: 添加 `close` 方法**

在 `_write_manifest` 方法之后添加：
```python
def close(self) -> None:
    """释放 MarkdownConverter 线程池资源。"""
    self._converter.close()
```

- [ ] **Step 5: 验证文件语法**

Run: `source activate govdoc-auditor-v3 && python -c "from govdoc.storage.files import DocumentStore; print('OK')"`

Expected: `OK`

- [ ] **Step 6: Commit**

```bash
git add govdoc/storage/files.py
git commit -m "refactor: DocumentStore 使用 MarkdownConverter 单例替代 to_markdown()"
```

---

### Task 3: 更新运行时装配（runtime.py + harness）

**Files:**
- Modify: `govdoc/runtime.py:37-40`
- Modify: `govdoc/harness/pipeline_eval.py:709`

- [ ] **Step 1: 修改 `runtime.py` 的 `get_document_store`**

将：
```python
@lru_cache
def get_document_store() -> DocumentStore:
    cfg = get_config()
    return DocumentStore(cfg.storage_root, ocr_backend=cfg.app.ocr_backend)
```
替换为：
```python
@lru_cache
def get_document_store() -> DocumentStore:
    cfg = get_config()
    return DocumentStore(cfg.storage_root, converter_kwargs=cfg.app.converter)
```

- [ ] **Step 2: 修改 `harness/pipeline_eval.py:709`**

将：
```python
store = DocumentStore(cfg.storage_root, ocr_backend=cfg.app.ocr_backend)
```
替换为：
```python
store = DocumentStore(cfg.storage_root, converter_kwargs=cfg.app.converter)
```

- [ ] **Step 3: 验证导入链**

Run: `source activate govdoc-auditor-v3 && python -c "from govdoc.runtime import get_document_store; print('OK')"`

Expected: `OK`

- [ ] **Step 4: Lint 检查**

Run: `source activate govdoc-auditor-v3 && ruff check govdoc/storage/files.py govdoc/config.py govdoc/runtime.py govdoc/harness/pipeline_eval.py`

Expected: 无错误（或仅已有的无关警告）

- [ ] **Step 5: Commit**

```bash
git add govdoc/runtime.py govdoc/harness/pipeline_eval.py
git commit -m "refactor: runtime 和 harness 适配 DocumentStore 新构造签名"
```

---

### Task 4: 重写单元测试

**Files:**
- Modify: `tests/unit/test_document_store.py`

- [ ] **Step 1: 重写整个测试文件**

完整替换 `tests/unit/test_document_store.py`：

```python
"""DocumentStore 单元测试：验证 MarkdownConverter 集成路径。"""

from __future__ import annotations

from pathlib import Path
from unittest.mock import MagicMock, patch

import pytest

from govdoc.storage.files import DocumentStore


@pytest.fixture()
def store(tmp_path: Path) -> DocumentStore:
    """创建使用临时目录的 DocumentStore（mock MarkdownConverter）。"""
    with patch("govdoc.storage.files.MarkdownConverter") as MockConverter:
        MockConverter.return_value = MagicMock()
        s = DocumentStore(tmp_path)
        yield s


class TestGetOrConvert:
    """get_or_convert 路由逻辑测试。"""

    def test_md_file_returns_directly(self, store: DocumentStore, tmp_path: Path) -> None:
        """已是 .md 的文件直接返回，不经过转换。"""
        md_file = tmp_path / "raw" / "test.md"
        md_file.parent.mkdir(parents=True, exist_ok=True)
        md_file.write_text("# Hello", encoding="utf-8")
        result = store.get_or_convert(md_file)
        assert result == md_file

    @pytest.mark.parametrize("suffix", [".docx", ".doc", ".pdf"])
    def test_supported_formats_call_converter(
        self, suffix: str, store: DocumentStore, tmp_path: Path
    ) -> None:
        """docx/doc/pdf 三种格式均调用 MarkdownConverter.convert。"""
        store._converter.convert.return_value = "# Converted content"

        raw_file = tmp_path / "raw" / f"test{suffix}"
        raw_file.parent.mkdir(parents=True, exist_ok=True)
        raw_file.write_bytes(b"fake content")

        result = store.get_or_convert(raw_file)

        store._converter.convert.assert_called_once_with(raw_file)
        assert result.exists()
        assert result.read_text(encoding="utf-8") == "# Converted content"

    def test_empty_result_raises(self, store: DocumentStore, tmp_path: Path) -> None:
        """MarkdownConverter.convert 返回空字符串时抛 RuntimeError。"""
        store._converter.convert.return_value = ""

        raw_file = tmp_path / "raw" / "test.docx"
        raw_file.parent.mkdir(parents=True, exist_ok=True)
        raw_file.write_bytes(b"fake docx")

        with pytest.raises(RuntimeError, match="返回空内容"):
            store.get_or_convert(raw_file)

    def test_conversion_error_propagates(self, store: DocumentStore, tmp_path: Path) -> None:
        """MarkdownConverter 抛出的 OSError 原样上抛，不被吞掉。"""
        store._converter.convert.side_effect = OSError("OCR unreachable")

        raw_file = tmp_path / "raw" / "test.pdf"
        raw_file.parent.mkdir(parents=True, exist_ok=True)
        raw_file.write_bytes(b"fake pdf")

        with pytest.raises(OSError, match="OCR unreachable"):
            store.get_or_convert(raw_file)

    def test_unsupported_format_fallback_text(self, store: DocumentStore, tmp_path: Path) -> None:
        """非文档格式降级为纯文本提取。"""
        raw_file = tmp_path / "raw" / "data.csv"
        raw_file.parent.mkdir(parents=True, exist_ok=True)
        raw_file.write_text("a,b,c\n1,2,3", encoding="utf-8")

        warnings_stack: list[str] = []
        result = store.get_or_convert(raw_file, warnings_stack=warnings_stack)

        assert result.exists()
        assert "a,b,c" in result.read_text(encoding="utf-8")
        assert any("不支持" in w for w in warnings_stack)

    def test_file_not_found_raises(self, store: DocumentStore) -> None:
        """原始文件不存在时抛 FileNotFoundError。"""
        with pytest.raises(FileNotFoundError, match="原始文件不存在"):
            store.get_or_convert("/nonexistent/file.docx")


class TestSha256Cache:
    """SHA256 缓存机制测试。"""

    def test_second_call_uses_cache(self, store: DocumentStore, tmp_path: Path) -> None:
        """相同内容的文件第二次调用不再触发 converter.convert。"""
        store._converter.convert.return_value = "# Cached"

        raw_file = tmp_path / "raw" / "test.docx"
        raw_file.parent.mkdir(parents=True, exist_ok=True)
        raw_file.write_bytes(b"same content")

        store.get_or_convert(raw_file)
        store.get_or_convert(raw_file)

        assert store._converter.convert.call_count == 1


class TestConverterIntegration:
    """验证 MarkdownConverter 构造与生命周期。"""

    @patch("govdoc.storage.files.MarkdownConverter")
    def test_converter_receives_config_kwargs(self, MockConverter: MagicMock, tmp_path: Path) -> None:
        """converter_kwargs 正确透传给 MarkdownConverter 构造函数。"""
        kwargs = {
            "mode": "api",
            "dispatch": "fallback",
            "monkey_endpoints": ["http://localhost:7866"],
        }
        DocumentStore(tmp_path, converter_kwargs=kwargs)

        MockConverter.assert_called_once_with(**kwargs)

    @patch("govdoc.storage.files.MarkdownConverter")
    def test_default_converter_no_kwargs(self, MockConverter: MagicMock, tmp_path: Path) -> None:
        """不传 converter_kwargs 时 MarkdownConverter 以空参构造。"""
        DocumentStore(tmp_path)

        MockConverter.assert_called_once_with()

    @patch("govdoc.storage.files.MarkdownConverter")
    def test_close_delegates_to_converter(self, MockConverter: MagicMock, tmp_path: Path) -> None:
        """close() 委托给 MarkdownConverter.close()。"""
        store = DocumentStore(tmp_path)
        store.close()

        MockConverter.return_value.close.assert_called_once()
```

- [ ] **Step 2: 运行测试验证全部通过**

Run: `source activate govdoc-auditor-v3 && python -m pytest tests/unit/test_document_store.py -v`

Expected: 全部通过（约 10 个测试）

- [ ] **Step 3: Commit**

```bash
git add tests/unit/test_document_store.py
git commit -m "test: 重写 DocumentStore 测试适配 MarkdownConverter API"
```

---

### Task 5: 更新手动测试脚本

**Files:**
- Modify: `scripts/test_pdf_convert.py`

- [ ] **Step 1: 重写 Step 4 部分**

将 `scripts/test_pdf_convert.py` 第 60-84 行（Step 4 部分）从：
```python
print("=" * 60)
print("Step 4: 通过 scrivai.to_markdown 转换 PDF")
print("=" * 60)

from scrivai import to_markdown

print(f"  PDF: {PDF_PATH}")
print(f"  文件存在: {Path(PDF_PATH).exists()}")
if Path(PDF_PATH).exists():
    print(f"  文件大小: {Path(PDF_PATH).stat().st_size / 1024 / 1024:.1f} MB")

print()
print("  开始转换...")
try:
    md = to_markdown(
        PDF_PATH,
        ocr_backend="mineru",
        max_workers=1,
        timeout=3600,
    )
    print(f"  ✓ 转换成功！长度: {len(md)} 字符")
    print(f"  前 300 字符:\n{md[:300]}")
except Exception as e:
    print(f"  ✗ 转换失败: {e}")
    traceback.print_exc()
```

替换为：
```python
print("=" * 60)
print("Step 4: 通过 MarkdownConverter 转换 PDF")
print("=" * 60)

from scrivai import MarkdownConverter

print(f"  PDF: {PDF_PATH}")
print(f"  文件存在: {Path(PDF_PATH).exists()}")
if Path(PDF_PATH).exists():
    print(f"  文件大小: {Path(PDF_PATH).stat().st_size / 1024 / 1024:.1f} MB")

print()
print("  开始转换...")
try:
    with MarkdownConverter(mode="gpu") as conv:
        md = conv.convert(PDF_PATH)
    print(f"  ✓ 转换成功！长度: {len(md)} 字符")
    print(f"  前 300 字符:\n{md[:300]}")
except Exception as e:
    print(f"  ✗ 转换失败: {e}")
    traceback.print_exc()
```

- [ ] **Step 2: Commit**

```bash
git add scripts/test_pdf_convert.py
git commit -m "refactor: test_pdf_convert.py 适配 MarkdownConverter API"
```

---

### Task 6: 全量验证 + 代码格式化

**Files:** 无新增修改

- [ ] **Step 1: 代码格式化**

Run: `source activate govdoc-auditor-v3 && ruff format govdoc/storage/files.py govdoc/config.py govdoc/runtime.py govdoc/harness/pipeline_eval.py tests/unit/test_document_store.py`

- [ ] **Step 2: Lint 检查**

Run: `source activate govdoc-auditor-v3 && ruff check govdoc/storage/files.py govdoc/config.py govdoc/runtime.py govdoc/harness/pipeline_eval.py tests/unit/test_document_store.py --fix`

Expected: 无错误

- [ ] **Step 3: 全量单元测试**

Run: `source activate govdoc-auditor-v3 && python -m pytest tests/unit/ -v`

Expected: 全部通过

- [ ] **Step 4: 版本确认**

Run: `source activate govdoc-auditor-v3 && pip show scrivai | head -3`

Expected: `Version: 0.2.3`

- [ ] **Step 5: 导入链端到端验证**

Run: `source activate govdoc-auditor-v3 && python -c "
from govdoc.runtime import get_document_store
store = get_document_store()
print('converter type:', type(store._converter).__name__)
print('OK')
"`

Expected:
```
converter type: MarkdownConverter
OK
```

- [ ] **Step 6: 如有格式化改动则 commit**

```bash
git add -u
git commit -m "style: ruff format 格式化"
```
